<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:KMA.ProgrammingInChsarp2026.LecturerManager.UIModels;assembly=LecturerManager.UIModels"
             x:Class="KMA.ProgrammingInCSharp2026.LecturerManager.Pages.DepartmentDetailsPage"
             x:DataType="models:DepartmentUIModel"
             Title="Department Details">
    <Grid RowDefinitions="Auto, *">
        <Border
            Stroke="Blue"
            Padding="10"
            Margin="0,10">
            <VerticalStackLayout>
                <Label
                Style="{StaticResource SubHeadline}">
                    <MultiBinding
                    StringFormat="{}{0} (Staff: {1})">
                        <Binding Path="Name"></Binding>
                        <Binding Path="StaffDesc"></Binding>
                    </MultiBinding>
                </Label>
                <Label
                Text="{Binding Faculty, Converter={StaticResource EnumToDisplayName}}"
                HorizontalOptions="Center"/>
            </VerticalStackLayout>
        </Border>
        <CollectionView Grid.Row="1"
                ItemsSource="{Binding Lecturers}"
                Margin="10"
                SelectionMode="Single"
                SelectionChanged="LecturerSelected">
            <CollectionView.ItemTemplate>
                <DataTemplate
                        x:DataType="models:LecturerUIModel">
                    <Border
                            Stroke="Magenta"
                            Padding="10"
                            Margin="0,10">
                        <VerticalStackLayout>
                            <Label
                                    Style="{StaticResource SubHeadline}">
                                <MultiBinding
                                        StringFormat="{}{0} {1}">
                                    <Binding Path="FirstName"></Binding>
                                    <Binding Path="LastName"></Binding>
                                </MultiBinding>
                            </Label>
                            <Label
                                    Text="{Binding Position, Converter={StaticResource EnumToDisplayName}}"
                                    HorizontalOptions="Center"/>
                        </VerticalStackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>